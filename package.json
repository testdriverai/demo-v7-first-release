{
  "name": "testdriver-vitest-example",
  "version": "1.0.0",
  "description": "Example project using TestDriver Vitest plugin",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "testdriverai": "^7.1.2"
  }
}
